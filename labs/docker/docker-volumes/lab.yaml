title: Docker Volumes
slug: docker-volumes
track: docker
description: Persist data beyond a container's lifetime using named volumes.
difficulty: intermediate
durationMinutes: 30
tags:
  - Volumes
  - Storage
  - Persistence
orderInTrack: 4
imageRef: skillsim/docker-volumes:latest
# Runs its own nested dockerd (Docker-in-Docker) — see terminal-server/src/docker.ts
# for what this exception to the default hardening profile actually changes.
privileged: true
