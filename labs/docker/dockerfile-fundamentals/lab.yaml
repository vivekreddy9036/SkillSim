title: Dockerfile Fundamentals
slug: dockerfile-fundamentals
track: docker
description: Write, build, and run your own image from a Dockerfile.
difficulty: beginner
durationMinutes: 30
tags:
  - Dockerfile
  - Images
  - Build
orderInTrack: 2
imageRef: skillsim/dockerfile-fundamentals:latest
# Runs its own nested dockerd (Docker-in-Docker) — see terminal-server/src/docker.ts
# for what this exception to the default hardening profile actually changes.
privileged: true
